package Simulation.entities.mobile;

public class Wolf extends Carnivore{
    public Wolf(int health, int movementSpeed, int attackDamage, int attackRadius) {
        super(health, movementSpeed, attackDamage, attackRadius);
    }
}
